spring.application.name=mycoolapp
management.endpoints.web.exposure.include=*
management.info.env.enabled=true

# Exclude indivisual endpoints with a comma-delimited list
management.endpoints.web.exposure.exclude=health,info

# passing data in "/actuator/info"
#
info.app.name=My super Cool App
info.app.description=A crazy fun App!
info.app.version=1.0.0

# Define my own properties
#
coach.name=Mickey mouse
team.name=The Mouse Club

# Change Spring Boot embedded server port
#
server.port = 7070

# Set the context path of the application
#
server.servlet.context-path=/mycoolapp
# That means all the requests should be prefixed with /mycoolapp or http://localhost:7070/mycoolapp/
